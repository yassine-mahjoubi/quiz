#on force lz redirect car note base url est "/quiz" qui est exigé par githubPages

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200