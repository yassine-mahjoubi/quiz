#force redirect car notre base url est "/quiz" qui est exigé par githubPages

[build]
  command = "npm run build-only"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200